# AudioFit ML — Python dependencies
# Core
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
scipy>=1.12

# Spotify & HTTP
requests>=2.31
python-dotenv>=1.0

# Embeddings (choose one or both)
# Lightweight TF-IDF path uses only sklearn (no extra install)
# For dense transformer embeddings, uncomment:
# sentence-transformers>=2.5
# torch>=2.2  # required by sentence-transformers

# API service
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Optional: vector search acceleration
# faiss-cpu>=1.7  # uncomment for ANN on large catalogs
